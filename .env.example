# Database (async driver for SQLAlchemy)
DATABASE_URL=postgresql+asyncpg://***:***@***:***/***?sslmode=require
DATABASE_SSL_NO_VERIFY=true

# Upstash Redis (optional – workflow cache; leave empty to disable)
# Create a database at https://console.upstash.com/ and copy REST URL + token
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Gemini (for step execution)
GEMINI_API_KEY=
# Optional: GEMINI_MODEL=gemini-2.5-flash
